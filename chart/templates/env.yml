apiVersion: v1
kind: ConfigMap
metadata:
  annotations: {}
  labels:
    {{- include "kruonis.labels" . | nindent 4 }}
  name: {{ include "kruonis.name" . }}-environment
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
